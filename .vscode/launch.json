{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "relations",
            "type": "python",
            "request": "attach",
            "port": 5678,
            "host": "localhost",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/opt/service"
                }
            ]
        }
    ]
}
